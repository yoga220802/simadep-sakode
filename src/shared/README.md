# Shared Boundary

Shared modules are generic primitives with no SIMADEP business vocabulary.

Move code here only when it is stable, generic, and useful across features. A project card is feature UI; pagination is shared UI.
